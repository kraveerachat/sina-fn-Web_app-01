'use client';

// ═══════════════════════════════════════════════════════════════════
// PulseBackground — retired particle canvas.
//
// The Apple-clean system carries ambience through the body's static
// radial gradient (--bg-grad in globals.css); an animated WebGL
// background is visual noise here, so this renders nothing. The
// component remains so layout.tsx keeps a stable import surface.
// ═══════════════════════════════════════════════════════════════════

export default function PulseBackground() {
  return null;
}
